
/* Function show lis */
const listMovies = (req, res) => {
  // res.render('movies', { title: 'Express' });
};

const movies = {
  index: listMovies,
  // movie: streamingMovie,
};

module.exports = movies;
